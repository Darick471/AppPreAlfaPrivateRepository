<Page
    x:Class="AppPreAlfa.Views.Login.PasswordStepView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AppPreAlfa.Views.Login"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <StackPanel
                Grid.Row="1"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">


            <TextBlock
                    FontStyle="Normal"
                    Text="Iniciar sesión"
                    FontSize="28"
                    FontFamily="Arial"
                    FontWeight="ExtraBold"
                    HorizontalTextAlignment="Center"
                    Margin="0,100,0,100"
                    Foreground="Black" />

            <TextBlock
                    FontStyle="Normal"
                    Text="Ingrese su contraseña"
                    FontSize="14"
                    FontWeight="ExtraBold"
                    HorizontalAlignment="Left"
                    Margin="15,5,0,0"
                    Foreground="Black" />

            <PasswordBox
                    x:Name="PasswordBox"
                    Password="{Binding Password, Mode=TwoWay}"
                    PasswordChanged="PasswordBox_PasswordChanged"
                    Grid.Column="0"
                    Background="White"
                    BorderBrush="White"
                    Height="60"
                    Width="350"
                    HorizontalAlignment="Center"
                    Margin=" 0,0,0,40"
                    Style="{StaticResource MaterialFilledPasswordBoxStyle}"
                    AutomationProperties.AutomationId="TextBox_Default_Placeholder_Material" />
            <TextBlock
                    x:Name="PasswordMismatchWarning"
                    Text="{Binding WarningMessage}"
                    Foreground="Red"
                    FontSize="12"
                    Visibility="{Binding WarningMessage, Converter={StaticResource StringToVisibilityConverter}}"
                    Margin="0,0,0,0"
                    TextWrapping="WrapWholeWords"
                    HorizontalAlignment="Left" />

            <Button
                    x:Name="SiguienteButton"
                    Margin="10, 10 ,8 ,8"
                    CornerRadius="5"
                    Height="50"
                    Width="350"
                    HorizontalAlignment="Center"
                    Content="Siguiente"
                    FontSize="16"
                    Command="{Binding SiguienteCommand}"
                    IsEnabled="{Binding SiguienteCommand.CanExecute}"/>
            <ProgressRing 
                    IsActive="{Binding IsValidating}"
                    Width="50"
                    Height="50"
                    HorizontalAlignment="Center"/>

            <TextBlock
                    FontStyle="Normal"
                    FontFamily="Arial"
                    Text="Al iniciar sesión aceptas nuestro aviso de &#x0a;privacidad y los terminos de uso"
                    TextAlignment="Center"
                    FontSize="10"
                    FontWeight="ExtraBold"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Bottom"
                    Margin="0,190,0,0"
                    Foreground="Gray" />

        </StackPanel>

    </Grid>
</Page>
